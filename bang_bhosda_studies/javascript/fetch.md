# fetch

microtask queue
- get mor priority

divided in two part

1. web browser/node
    network request
        if responce came - resolve
        if didnt came - rejection 

2. variable and data to reserve space for data
    onfullfiled[fn] -resolve
    onrejection[fn] - rejection
     not allow push value (privet field)
